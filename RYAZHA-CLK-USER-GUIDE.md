# Ryazha-clk v3.0.0 — полный гайд по кнопкам и настройкам

> Этот документ объясняет **что делает каждый видимый пункт Ryazha-clk**, где он находится, когда он применяется и от каких условий зависит. Описание проверено по актуальному дереву исходников Ryazha-clk: `Source/hoc-clk/overlay/src/ui/gui/` и `Source/hoc-clk/sysmodule/src/`.
>
> **Важно:** разгон, напряжения, частота экрана, зарядный ток и чиптюнинг могут привести к нестабильности, перегреву или отказу загрузки. Начинайте с `Авто`, меняйте один параметр за раз и проверяйте работу игры. Если настройки KIP сделали систему нестабильной, используйте «Сброс кипа» либо восстановите рабочий `rcu.kip` на SD-карте.

---

## 1. Как читать меню

| Обозначение | Что означает |
|---|---|
| **A** | Открыть подменю, выбрать значение или переключить пункт. |
| **B** | Вернуться назад без нового выбора. |
| **Y** на CPU/GPU/RAM в профиле или Overrides | Сбросить только эту частоту к `0` — снова действуют нижестоящие правила. |
| **X** на главном экране | Открыть скрытое меню **«Ряженка-Авто»**. |
| **0 / Авто / RCU-Авто** | Не фиксировать ручное значение; решение оставляется таблицам и автоматике Ryazha-clk. |
| **«после перезагрузки»** | Параметр записывается в KIP и полностью вступает в силу после перезапуска консоли. |
| **«живьём»** | Значение уходит в sysmodule по IPC и применяется без перезагрузки. |
| **Справка по пункту** | В меню доступна отдельная справка; она выводит текст, связанный с ключом конфигурации. |

Не путайте **сохранённый профиль**, **глобальный профиль** и **временное значение**. Приоритет простой: временный override действует поверх профиля, профиль игры — поверх общего профиля, а `0` передаёт управление следующему уровню.

---

## 2. Главный экран

| Кнопка | Что делает на самом деле | Где это в коде |
|---|---|---|
| **Профиль игры** | Открывает сохранённые частоты и настройки governor только для запущенной игры. Профиль привязан к её `applicationId`. | `main_gui.cpp:57–66`, `app_profile_gui.cpp:503–515` |
| **Общий профиль** | Открывает профиль с глобальным ID. Он служит значением по умолчанию для всех игр, у которых нет собственного значения. | `main_gui.cpp:68–77` |
| **Временные значения** | Открывает override, который отправляется в sysmodule напрямую. Он не заменяет сохранённый профиль и снимается сбросом/перезапуском. | `main_gui.cpp:79–88`, `global_override_gui.cpp:386–439` |
| **Настройки** | Открывает системные разделы: разгон, экран, безопасность, governor и экспериментальные функции. | `main_gui.cpp:92–101` |
| **О программе** | Показывает сведения о версии и сборке. На частоты и напряжения не влияет. | `main_gui.cpp:103–112` |
| **X: Ряженка-Авто** | Открывает автоматический разгон по FPS. Режим использует FPS от SaltyNX и сам двигает CPU/GPU в заданных границах. | `main_gui.cpp:127–137`, `misc_gui.cpp:2184–2278` |

> Пункт обновления намеренно отсутствует: встроенный апдейтер апстрима мог бы предложить заменить Ryazha-clk чужой сборкой. Обновление выполняется через release Ряженки/Ryazha-clk.

---

## 3. Профиль игры и общий профиль

Каждый профиль содержит отдельные секции для **дока**, **портатива** и поддерживаемых режимов зарядки. Для каждой секции используются одинаковые строки.

| Пункт | Что сохраняется | Поведение |
|---|---|---|
| **ЦПУ** | Целевая частота CPU для текущего режима. | A открывает список реальных доступных частот; Y записывает `0` и возвращает управление общему профилю/автоматике. |
| **ГПУ** | Целевая частота GPU. | Список учитывает выбранную таблицу GPU и реально доступные ступени. |
| **РАМ** | Целевая частота памяти EMC. | Не задаёт VDD2/VDDQ сам по себе: ручные напряжения памяти не затираются. |
| **Дисплей** | Целевая герцовка профиля. | Появляется только если включено «Менять частоту обновления». Ограничения зависят от LCD/OLED, дока и режима Retro. |
| **Говернёр** | Режимы governor для CPU, GPU и VRR. | Упакован в одно поле профиля: `Не менять`, `Выкл`, `Вкл`; у VRR есть четвёртый режим **VRR-Авто**. |

### Governor в профиле

| Строка | Режимы | Значение |
|---|---|---|
| **ЦПУ** | Не менять / Выкл / Вкл | Включает или отключает управление CPU-governor для данного профиля. |
| **ГПУ** | Не менять / Выкл / Вкл | То же для GPU-governor. |
| **VRR** | Не менять / Выкл / Вкл / VRR-Авто | Управляет VRR данного профиля. Строка появляется, когда разрешена перезапись частоты дисплея либо включен режим Retro. |

Код профиля пишет изменения через `hocclkIpcSetProfiles`; поэтому профиль остаётся после перезапуска. Логика экрана и packed governor находится в `app_profile_gui.cpp:296–501`.

---

## 4. Временные значения

**Временные значения** повторяют CPU/GPU/RAM/Display/Governor, но работают иначе.

| Пункт | Что происходит |
|---|---|
| **ЦПУ / ГПУ / РАМ** | A задаёт частоту только на текущий запуск. Y сбрасывает override в `0`. |
| **Дисплей** | Если разрешена смена герцовки, ползунок отправляет временный Hz через `hocclkIpcSetOverride`. |
| **Governor** | Временно задаёт CPU/GPU/VRR governor; профиль не переписывается. |

Это правильное место для проверки, выдержит ли конкретная игра частоту, прежде чем сохранять её в профиле. Реализация использует `hocclkIpcSetOverride`, а не `hocclkIpcSetProfiles` — см. `global_override_gui.cpp:327–439`.

---

## 5. Настройки → Разгон → ЦПУ

### Частота и потолок

| Пункт | Ключ / диапазон | Что делает |
|---|---|---|
| **Частота ЦПУ** | `commonCpuBoostClock`, до 2907 МГц | Выбирает потолок CPU DVFS-лестницы после перезагрузки. Список фильтруется по реальному доступному напряжению: недостижимые точки не предлагаются. |
| **Макс. вольтаж ЦПУ** | `marikoCpuMaxVolt`, Авто или 1130–1375 мВ | Верхняя граница CPU-рейла. При выборе более высокой частоты меню автоматически поднимает потолок как минимум до требуемого значения. Понижение потолка принудительно ограничивает ручной Vmin, чтобы минимум не стал выше максимума. |
| **Мин. вольтаж ЦПУ** | `CPUv` | Выбирает источник нижнего напряжения CPU: авто по RAM, авто с −20/−40 мВ или собственная таблица. |
| **Задать вручную (мВ)** | то же поле `CPUv` | Ручной Vmin CPU. Работает вместо предыдущего пункта, ограничивается потолком CPU и диапазоном 500–1000 мВ. На высокой RAM sysmodule может поднять его до обязательного пола. |
| **DVCO верх** | `marikoCpuHUV`, 0–12 | Верхняя ступень CPU DVCO. `0` — автоматический выбор. Более высокая ступень открывает высокие CPU-точки, но требует достаточного потолка напряжения. |
| **DVCO низ** | `marikoCpuUV`, 0–12 | Нижняя ступень CPU DVCO. Обычно оставляется `0`: ненулевое значение меняет CPU-таблицу и не является обычным способом разгона. |
| **Логика Min-High ЦПУ** | `dCPUv`, 0–3 | Выбирает режим внутренней автоматической логики Vmin CPU. |
| **Андервольт низа ЦПУ** | `SpeedShift` | Сдвигает нижнюю часть CPU-таблицы. Допустимы только готовые кратные 25 мВ варианты; произвольные шаги не используются, так как нарушают сетку загрузочной таблицы. |

### CPU и RAM связаны

При разгоне памяти sysmodule вычисляет **минимальный безопасный пол CPU по частоте RAM**. Поэтому ручное низкое напряжение CPU не всегда применяется буквально: если оно ниже обязательного пола для текущей EMC-частоты, sysmodule поднимет его на лету. Экран «Таблица вольтажа ЦПУ» показывает этот пол в подсказке.

| Пункт | Что делает |
|---|---|
| **Включить буст от РАМ** | При RAM выше заданного порога удерживает CPU на выбранной частоте. Система отключается, если используется Ряженка-Авто, чтобы две автоматики не спорили. |
| **Порог РАМ** | RAM-частота, после которой включается CPU-буст. `Авто` выбирает середину реальной лестницы памяти данной консоли. |
| **Бустить ЦПУ до** | Частота CPU, которую механизм RAM→CPU будет удерживать при срабатывании. |
| **Таблица вольтажа ЦПУ** | Открывает 24 частотные точки 510–2907 МГц. Каждая точка имеет окно около дефолта; запись идёт в KIP. Низкие точки могут быть подняты обязательным полом RAM. |

Исходник: `misc_gui.cpp:842–1177`, `misc_gui.cpp:1688–1760`.

---

## 6. Настройки → Разгон → ГПУ

Эти пункты относятся к **Mariko GPU**. Они формируют частотную/вольтажную таблицу GPU и применяются после перезагрузки, кроме таблицы ручного режима.

| Пункт | Что делает | Важная зависимость |
|---|---|---|
| **Режим ГПУ** | Выбирает доступную GPU-таблицу: до 1459, 1536, 1766 МГц либо «Ручная таблица». | «Ручная таблица» использует отдельную таблицу напряжений GPU. |
| **Кривая вольтажа** | Выбирает автоматическую форму кривой или ручную нижнюю точку 500–900 мВ. | Не действует, если «Режим ГПУ» = «Ручная таблица». |
| **Вольтаж вершины** | Задаёт напряжение верхней GPU-точки: Авто либо 750–1200 мВ. | На высоких частотах зависит от качества чипа; не заменяет «Потолок рейла». |
| **Потолок рейла** | Верхняя граница GPU-рейла. При Авто используется стоковая граница около 1050 мВ. | Точки выше практического лимита sysmodule всё равно ограничивает; ручная таблица не может выйти за допустимый диапазон. |
| **Пол вольтажа** | Нижняя граница Vmin GPU; Авто или 300–900 мВ. | Нельзя задать точку ручной таблицы ниже этого пола. |
| **UV нижней точки** | Уменьшает нижнюю часть GPU-кривой на 5–45 мВ. | Не работает в «Ручной таблице». |
| **UV вершины** | Уменьшает верхнюю часть GPU-кривой на 5–45 мВ. | Не работает в «Ручной таблице». |
| **Таблица вольтажа ГПУ** | 31 ручная точка от 307 до 1766 МГц. | Предназначена для «Ручной таблицы». Диапазон каждого ползунка ограничен полом и потолком GPU-рейла. |
| **Сбросить к кривой RCU** | Восстанавливает все 31 точки к дефолтам RCU. | Применяется через IPC сразу для ручной таблицы. |

Исходник: `misc_gui.cpp:1180–1273`, `misc_gui.cpp:1613–1680`.

---

## 7. Настройки → Разгон → РАМ, DVFS и напряжения памяти

### Частота RAM

| Пункт | Что делает |
|---|---|
| **Макс. частота РАМ** | Задаёт максимальную частоту EMC после перезагрузки. Выбор частоты **не перезаписывает** ваши ручные VDD2/VDDQ. При `Авто` лоадер использует собственный подбор. |
| **Тайминги MTC** | Выбирает: автонастройку, свои поправки, не менять тайминги или собственную таблицу. |
| **Запас записи** | Увеличивает вклад в `nWR`: выше значение обычно свободнее и стабильнее, но не быстрее. |
| **Ужатие записи** | Вычитает вклад из `nWR`: выше значение сильнее ужимает запись и увеличивает риск. |
| **Тайминги памяти** | Открывает расширенные тайминги, pMEH/sMEH и SHAITAN. Это экспертная зона. |

### Живой вольтаж — это DVFS / PCV Hijack

> **«Живой вольтаж» — не просто отображение.** При включении он переводит `DVFSMode` в `PCV Hijack`: sysmodule перед повышением EMC применяет VDD2/VDDQ для целевой ступени RAM и корректирует связанный Vmin GPU. При выключении используются общие VDD2/VDDQ, а по-частотная таблица скрывается.

| Состояние | Какие пункты доступны | Как работает |
|---|---|---|
| **Живой вольтаж = Выкл** | **VDD2 памяти**, **VDDQ шины**, **Вольтаж SoC** | Одно глобальное значение VDD2/VDDQ действует на все частоты памяти. `Авто` даёт лоадеру подобрать напряжение. |
| **Живой вольтаж = Вкл** | **Сдвиг Vmin ГПУ при RAM**, **Таблица вольтажа РАМ**, **Вольтаж SoC** | VDD2/VDDQ выбираются отдельно для текущей ступени EMC. Общие VDD2/VDDQ намеренно скрываются, потому что в этом режиме не участвуют. |

| Пункт | Что делает |
|---|---|
| **Сдвиг Vmin ГПУ при RAM** | Новый видимый `DVFSOffset`. Сдвигает рассчитанный Vmin GPU при переходе частоты RAM в режиме PCV Hijack. Диапазон UI: **−80…+80 мВ**, шаг 5 мВ; `0` — без сдвига. Вне «Живого вольтажа» пункт скрыт и не работает. |
| **VDD2 памяти** | Глобальное питание памяти при выключенном живом режиме. Сетка физического преобразователя — 12,5 мВ; `Авто` рекомендуется по умолчанию. |
| **VDDQ шины** | Глобальное питание интерфейса DRAM при выключенном живом режиме. |
| **Вольтаж SoC** | Глобальный сдвиг/ручное значение SoC. Сам секвенсер уже использует собственную по-частотную DVB-таблицу. |
| **Таблица вольтажа РАМ** | При включённом живом режиме показывает реальные доступные частоты памяти. Для каждой ступени есть пара **VDD2** и **VDDQ**. `Авто` в ячейке возвращает авто-брекет. |

Пофреквенционная таблица содержит до 16 ступеней — это ограничение формата конфигурации. VDD2 в таблице начинается с 1125 мВ, VDDQ — с 500 мВ: более низкие значения sysmodule всё равно ограничил бы до физического пола.

Исходник: `misc_gui.cpp:1276–1472`, `misc_gui.cpp:1762–1821`, `clock_manager.cpp:669–717` и `clock_manager.cpp:962–1044`.

---

## 8. Настройки → Разгон → Чиптюнинг

Чиптюнинг меняет параметры DC/DC-преобразователей. Он не нужен для обычного разгона и должен оставаться выключенным, если нет конкретной причины и проверки стабильности.

| Пункт | Что делает |
|---|---|
| **Включить** | Глобально включает применение параметров чиптюнинга. Без него остальные значения не должны рассматриваться как активная настройка. |
| **Прямой ШИМ** | Переводит соответствующий рейл CPU/DRAM/GPU в режим forced PWM. На Mariko имеются отдельные CPU M4, DRAM M3 и GPU M1; на Erista — отдельные CPU/GPU-переключатели. |
| **Лимит тока** | Пиковый лимит выбранного рейла. В UI значения 7,2 A и 8 имеют предупреждающие пороги. |
| **Частота ШИМ** | Частота переключения преобразователя: Авто или 1,0–2,5 МГц. |
| **Компенсация петли** | Включает компенсацию управляющей петли для рейлов. |
| **Скорость** | Профиль скорости нарастания напряжения всех рейлов: Авто, 5/10/20/40 мВ/мкс. Агрессивные варианты могут усиливать переходные процессы. |
| **Напряжение картриджа** | Питание GCA: Авто либо 2800–3300 мВ; 3100 мВ отмечено как HOS default. |

Исходник: `misc_gui.cpp:1825–1918`.

---

## 9. Настройки → Экран / VRR

| Пункт | Что делает | Условия |
|---|---|---|
| **Менять частоту обновления** | Разрешает профилям и Overrides менять Hz дисплея. | Без него не появятся строки Display/VRR в нужных местах. |
| **Настройки VRR-Авто** | Открывает параметры умного VRR. Сам режим включается в профиле или Override → Governor → VRR → **VRR-Авто**. | Нужен SaltyNX для FPS; только портатив. |
| **Макс. Гц (портатив)** | Верхняя граница ручной портативной герцовки. | LCD: до 75 Гц; OLED/Aula: консервативный экранный диапазон. Несовместимые значения могут повредить матрицу. |
| **Вольтаж дисплея** | Напряжение панели 800–1200 мВ. | Скрыто на Aula; риск для LCD-панели. |

### VRR-Авто

| Пункт | Что делает |
|---|---|
| **Мин. Гц / Макс. Гц** | Нижняя и верхняя границы, внутри которых VRR следует за FPS. LCD использует 40–75 Гц; OLED имеет собственные ограничения. |
| **Агрессивность** | Насколько охотно предиктор меняет герцовку. |
| **Порог скачка** | Разница FPS, которую алгоритм считает резким изменением. |
| **Запас над FPS** | На сколько Гц держать дисплей выше измеренного FPS. |
| **Задержка понижения** | Сколько тиков ждать перед снижением Hz, чтобы герцовка не прыгала на кратких просадках. |

Исходник: `misc_gui.cpp:2107–2182`, `misc_gui.cpp:2284–2318`.

---

## 10. Настройки → Общие, Говернёр и Безопасность

### Общие настройки

| Пункт | Что делает |
|---|---|
| **Вольтаж РАМ: показ** | На Mariko выбирает, какое значение RAM показывать в интерфейсе: VDD2 или VDDQ. На питание не влияет. |
| **Единицы частоты РАМ** | Форматирует отображение памяти как МГц, MT/s или оба варианта. На частоту не влияет. |
| **Интервал опроса** | Частота обновления информации sysmodule/оверлеем: 50–1000 мс. Меньше интервал — чаще опрос и выше служебная нагрузка. |

### Говернёр

| Пункт | Что делает |
|---|---|
| **Мин. частота ЦПУ** | Нижняя частота, которую CPU-governor будет удерживать при включённом governor. Доступны 510–1020 МГц. Это не фиксированная частота и не разгонный потолок. |

### Безопасность

| Пункт | Что делает |
|---|---|
| **Снять потолки частот** | Убирает программные ограничения частот. Использовать только после проверки остальных напряжений, температур и таблиц. |
| **Сброс при перегреве** | Включает защитный сброс/откат при превышении температуры. Рекомендуется держать включённым. |
| **Порог троттлинга** | Температура 50–85 °C, после которой начинает работать тепловая защита. Порог 70+ помечается предупреждением. |

Исходник: `misc_gui.cpp:1920–1967`, `misc_gui.cpp:2081–2105`, `misc_gui.cpp:2320–2343`.

---

## 11. Экспериментальные функции

Сначала на странице **Настройки** включите **«Экспериментальные функции»**. Только после этого появляется **«Открыть раздел»**.

| Пункт | Что делает | Ограничение |
|---|---|---|
| **Mariko Middle Frequencies** | Включает промежуточные ступени частот Mariko. | Только Mariko. |
| **Live CPU UV** | Включает экспериментальный живой undervolt CPU. | Только Mariko; требует особенно осторожной проверки. |
| **Планировщик ГПУ** | Выбирает метод override: INI или NV Service. | Меняет путь доставки политики GPU, а не GPU-частоту сам по себе. |
| **Измерение частоты РАМ** | Источник измерения EMC: PLL либо Actmon. | Влияет на измерение/отчёт, не является разгоном напрямую. |
| **Ток зарядки** | Меняет зарядный ток. | Имеет собственные лимиты Lite/других моделей; высокие значения отмечены предупреждением. |
| **Предел тока зарядки** | Меняет ограничение входного тока. | UI прямо предупреждает о нагрузке; до 1500 мА — обозначенный безопасный максимум подсказки. |
| **Цветовой профиль** | Меняет цветовой preset Aula/OLED. | Только Aula. |

Исходник: `misc_gui.cpp:1969–2079`.

---

## 12. Ряженка-Авто: авторазгон по FPS

Открывается **X с главного экрана**. Это отдельный автоматический режим, а не постоянный профиль частоты.

| Пункт | Что делает |
|---|---|
| **Включить авторазгон** | Включает движок Auto OC. Без валидного FPS от SaltyNX он не может правильно подбирать частоты. |
| **Целевой FPS** | Цель алгоритма: 30, 45, 60 или верхний режим панели. |
| **Допуск (кадров)** | Допустимое отклонение FPS от цели до следующего изменения частот. |
| **Алгоритм** | `Цикл` или `Смарт` — стратегия подбора частот. |
| **Лимит темп. ЦП / ГП** | Ограничения, которые важнее цели FPS. При достижении авторазгон снижает нагрузку. |
| **Лимит TDP** | Предел мощности: Авто либо 5–12 Вт. Он также важнее цели FPS. |
| **Мин./Макс. ЦП** | Нижняя и верхняя границы CPU, внутри которых автоматике разрешено искать частоту. |
| **Мин./Макс. ГП** | То же для GPU. |
| **Держать РАМ на максимуме** | Не позволяет автоматике отпускать RAM ниже максимума. Увеличивает нагрузку и может быть излишним для лёгких игр. |

Скрытый вход и UI: `main_gui.cpp:127–137`, `misc_gui.cpp:2186–2278`. Сам алгоритм расположен в `sysmodule/src/mgr/auto_oc.cpp`.

---

## 13. Расширенные тайминги и экспертные таблицы

Эти пункты не являются готовыми пресетами. Они напрямую меняют поля таблиц памяти и применяются пользователю, который понимает назначение каждого тайминга.

| Раздел / кнопка | Что меняется |
|---|---|
| **Тугость таймингов** | Главный уровень, который включает группы ужесточений MTC. Выше = обычно ниже задержки, но выше риск. |
| **tRCD / tRP / tRAS** | Прямое уменьшение соответствующих RAM-таймингов в циклах. |
| **SRPD / WL набор** | Дополнительные параметры self-refresh/write leveling. |
| **tRCDw / tWTR / tRTW / tWTP** | Сдвиги конкретных таймингов записи/чтения. |
| **Тонкая настройка (pMEH)** | Живые микро-параметры: nWR, запись, чтение, tRTW, tWTR, tWTP, divMB, вариант DVB, PLL divN, релакс и запас EMC-брекета. |
| **Вторичные тайминги (sMEH)** | ARB, latency, eZQ, tr DVFS, DBI, refresh, MRS и SSC. |
| **SHAITAN** | Ручной сплайсер PCV. Используется только для адресного патчинга слов PCV: число активных сплайсов, поиск, смещение, ожидаемое слово и замена. При нулях ничего не патчит. |

Исходник намеренно не показывает мёртвые слоты массивов. Это не «пропавшие настройки»: строки, которые лоадер не читает, скрыты, чтобы пользователь не менял неработающие значения. См. `misc_gui.cpp:1475–1610`.

---

## 14. Быстрые ответы на частые вопросы

| Вопрос | Короткий ответ |
|---|---|
| Где DVFS? | **Разгон → РАМ → Живой вольтаж**. Это переключатель PCV Hijack/DVFS. |
| Где GPU Vmin, зависящий от RAM? | При живом вольтаже: **Сдвиг Vmin ГПУ при RAM**. Базовый пол GPU находится в **Разгон → ГПУ → Пол вольтажа**. |
| Почему нет VDD2/VDDQ? | Включён живой вольтаж: вместо общих значений используйте **Таблицу вольтажа РАМ**. |
| Почему «Кривая вольтажа» GPU не меняет ничего? | Проверьте, не выбран ли **Режим ГПУ → Ручная таблица**: в нём кривая отключается. |
| Почему значение CPU ниже не применяется? | Высокая RAM может поднять CPU до обязательного пола по памяти. |
| Как быстро проверить частоту? | Используйте **Временные значения**, а не профиль игры. |
| Как вернуть частоты профиля? | На строке CPU/GPU/RAM нажмите **Y**; значение станет `0`. |
| Как сбросить KIP-настройки? | **Настройки → Сброс кипа**. Это сбрасывает пакет Ryazha-контролов к дефолтам. |

---

## 15. Карта исходного кода

| Что описано | Исходный файл |
|---|---|
| Главный экран и скрытый вход X | `overlay/src/ui/gui/main_gui.cpp` |
| Профили и governor профиля | `overlay/src/ui/gui/app_profile_gui.cpp` |
| Временные значения | `overlay/src/ui/gui/global_override_gui.cpp` |
| Все разделы настроек, CPU/GPU/RAM/тайминги/VRR/Auto OC | `overlay/src/ui/gui/misc_gui.cpp` |
| Справки и дефолты UI | `overlay/src/ui/gui/config_info_strings.cpp` |
| Переходы RAM/DVFS/GPU Vmin | `sysmodule/src/mgr/clock_manager.cpp` |
| Напряжения рейлов, VDD2/VDDQ и физические ограничения | `sysmodule/src/board/board_volt.cpp` |
| Авторазгон по FPS | `sysmodule/src/mgr/auto_oc.cpp` |

> Если названия пунктов или диапазоны изменятся в следующем релизе, ориентируйтесь прежде всего на этот документ, новый release и соответствующий UI-код. Гайд описывает Ryazha-clk **v3.0.0** с видимым регулятором `DVFSOffset`.


---

## 16. Исчерпывающий реестр строк меню и конфигурационных полей

Этот раздел намеренно **не объединяет параметры в общие фразы**. Каждая строка ниже соответствует отдельной кнопке, ползунку либо динамически создаваемому слоту UI. Если настройка имеет аппаратно-зависимое число строк, это прямо указано.

### 16.1. Полный реестр верхнего уровня

| Путь | Отдельная строка | Поле/действие | Когда доступна |
|---|---|---|---|
| Главный экран | Профиль игры | Открывает `HocClkTitleProfileList` текущего `applicationId`. | При запущенном приложении. |
| Главный экран | Общий профиль | Открывает профиль `HOCCLK_GLOBAL_PROFILE_TID`. | Всегда. |
| Главный экран | Временные значения | Пишет override через `hocclkIpcSetOverride`; не сохраняет профиль. | Всегда. |
| Главный экран | Настройки | Открывает `MiscGui`. | Всегда. |
| Главный экран | О программе | Только экран информации. | Всегда. |
| Главный экран | **X** | Открывает `AutoOcSubMenuGui`. | Только на главном экране. |
| Настройки | ЦПУ | Открывает `RyazhaCpuSubMenuGui`. | Всегда. |
| Настройки | ГПУ | Открывает `RyazhaGpuSubMenuGui`. | Всегда; фактические таблицы зависят от модели. |
| Настройки | РАМ | Открывает `RyazhaRamSubMenuGui`. | Всегда. |
| Настройки | Чиптюнинг | Открывает `ChiptuningSubMenuGui`. | Всегда; набор строк различается у Mariko/Erista. |
| Настройки | Сброс кипа | Вызывает `applyBundle(ryazhaResetBundle())`, возвращая KIP-пакет Ryazha к дефолтному набору. | Всегда. |
| Настройки | Дисплей | Открывает `DisplaySubMenuGui`. | Всегда; часть пунктов зависит от модели. |
| Настройки | Общие настройки | Открывает `GeneralSettingsSubMenuGui`. | Всегда. |
| Настройки | Говернёр | Открывает `GovernorSettingsSubMenuGui`. | Всегда. |
| Настройки | Безопасность | Открывает `SafetySubMenuGui`. | Всегда. |
| Настройки | Экспериментальные функции | `HocClkConfigValue_EnableExperimentalSettings`: включает показ экспериментального раздела и пересобирает список. | Всегда. |
| Настройки | Открыть раздел | Открывает `ExperimentalSettingsSubMenuGui`. | Только после включения предыдущего пункта. |

### 16.2. Профили, временные значения и Governor — каждая строка

| Экран | Строка | Действие записи | Сброс/условие |
|---|---|---|---|
| Профиль игры / Общий профиль | ЦПУ | Записывает MHz выбранного режима профиля в `mhzMap[profile][CPU]`. | Y записывает `0`; тогда применяется нижестоящее правило. |
| Профиль игры / Общий профиль | ГПУ | Записывает MHz в `mhzMap[profile][GPU]`. | Y записывает `0`. |
| Профиль игры / Общий профиль | РАМ | Записывает MHz в `mhzMap[profile][MEM]`. | Y записывает `0`. |
| Профиль игры / Общий профиль | Дисплей | Записывает Hz в `mhzMap[profile][Display]`; у портатива копирует Hz и в зарядочные профили. | Только при «Менять частоту обновления». |
| Профиль игры / Общий профиль → Говернёр | ЦПУ | Младший байт packed-поля `HocClkModule_Governor`: не менять / выкл / вкл. | Всегда. |
| Профиль игры / Общий профиль → Говернёр | ГПУ | Байты 8–15 того же packed-поля: не менять / выкл / вкл. | Всегда. |
| Профиль игры / Общий профиль → Говернёр | VRR | Байты 16–23: не менять / выкл / вкл / VRR-Авто. | Появляется, если разрешена смена герцовки, RetroSuper либо док-профиль. |
| Временные значения | ЦПУ | `hocclkIpcSetOverride(CPU, Hz)`. | Y сбрасывает override в `0`. |
| Временные значения | ГПУ | `hocclkIpcSetOverride(GPU, Hz)`. | Y сбрасывает override в `0`. |
| Временные значения | РАМ | `hocclkIpcSetOverride(MEM, Hz)`. | Y сбрасывает override в `0`. |
| Временные значения | Дисплей | `hocclkIpcSetOverride(Display, Hz)`. | Только при включённой смене Hz. |
| Временные значения → Governor | ЦПУ | Младший байт временного packed governor. | Значение не сохраняется в профиль. |
| Временные значения → Governor | ГПУ | Байты 8–15 временного packed governor. | Значение не сохраняется в профиль. |
| Временные значения → Governor | VRR | Байты 16–23 временного packed governor. | Появляется при доступном VRR. |

### 16.3. CPU — полный построчный реестр

| Строка | Ключ | Реальное действие кода | Диапазон / условие |
|---|---|---|---|
| Частота ЦПУ | `KipConfigValue_commonCpuBoostClock` | Выбирает верхнюю точку CPU DVFS; при необходимости автоматически поднимает `marikoCpuMaxVolt` до требуемого минимума. | Список от 1581 до 2907 МГц фильтруется действующим потолком CPU. |
| Макс. вольтаж ЦПУ | `KipConfigValue_marikoCpuMaxVolt` | Верхняя граница CPU-рейла. При снижении автоматически ограничивает ручной `CPUv`, если он оказался выше потолка. | Авто или 1130–1375 мВ, шаг 5 мВ. |
| Мин. вольтаж ЦПУ | `KipConfigValue_CPUv` | Выбирает режим Vmin: авто по RAM, авто −20, авто −40 либо собственная таблица. | Значения 3/2/1/0 соответственно. |
| Задать вручную (мВ) | `KipConfigValue_CPUv` | Пишет в то же поле прямой ручной Vmin вместо режима выше. | 500 мВ до меньшего из 1000 мВ и CPU-потолка; ниже пола RAM демон поднимет. |
| DVCO верх (0=Авто) | `KipConfigValue_marikoCpuHUV` | Выбирает верхнюю ступень DVCO CPU. | 0–12; 0 — автоматический выбор. |
| DVCO низ (0=Авто, обычно 0) | `KipConfigValue_marikoCpuUV` | Выбирает нижнюю ступень DVCO и способен переключить CPU-таблицу. | 0–12; для обычного случая сохраняют 0. |
| Логика Min-High ЦПУ | `KipConfigValue_dCPUv` | Выбирает стадию внутренней автоматической логики CPU Vmin. | 0–3. |
| Андервольт низа ЦПУ | `KipConfigValue_SpeedShift` | Сдвигает низ CPU-таблицы готовой корректной сеткой. | Только: ступень вниз, без изменений, −50, −25, +25, +50 мВ. |
| Включить буст от РАМ | `HocClkConfigValue_AutoRAMCPUOverclock` | Включает условное удержание CPU после достижения RAM-порога. | Отключается автоматикой при активном Auto OC. |
| Порог РАМ | `HocClkConfigValue_AutoRamCpuRamOCThreshold` | Порог EMC, при котором запускается предыдущий CPU-буст. | Авто = середина реальной RAM-лестницы либо доступная ступень. |
| Бустить ЦПУ до | `HocClkConfigValue_AutoRamCpuCpuOCFreq` | Частота CPU, применяемая механизмом RAM→CPU. | Только доступные частоты CPU. |
| Таблица вольтажа ЦПУ | `KipConfigValue_cpuVolt_First + i` | Открывает 24 отдельные CPU-точки; каждой соответствует своя строка ниже. | KIP; точка ниже обязательного пола RAM поднимается демоном. |

### 16.4. CPU-таблица — каждая отдельная частотная строка

Каждая строка задаёт Vmin в милливольтах для **одной конкретной CPU DVFS-точки**. UI ограничивает каждую точку окном примерно ±150 мВ вокруг дефолта и пределами 500–1375 мВ.

| Индекс | Отдельная строка UI | Конфигурационное поле |
|---:|---|---|
| 0 | 510 МГц | `cpuVolt_First + 0` |
| 1 | 612 МГц | `cpuVolt_First + 1` |
| 2 | 714 МГц | `cpuVolt_First + 2` |
| 3 | 816 МГц | `cpuVolt_First + 3` |
| 4 | 918 МГц | `cpuVolt_First + 4` |
| 5 | 1020 МГц | `cpuVolt_First + 5` |
| 6 | 1122 МГц | `cpuVolt_First + 6` |
| 7 | 1224 МГц | `cpuVolt_First + 7` |
| 8 | 1326 МГц | `cpuVolt_First + 8` |
| 9 | 1428 МГц | `cpuVolt_First + 9` |
| 10 | 1581 МГц | `cpuVolt_First + 10` |
| 11 | 1683 МГц | `cpuVolt_First + 11` |
| 12 | 1785 МГц | `cpuVolt_First + 12` |
| 13 | 1887 МГц | `cpuVolt_First + 13` |
| 14 | 1963 МГц | `cpuVolt_First + 14` |
| 15 | 2091 МГц | `cpuVolt_First + 15` |
| 16 | 2193 МГц | `cpuVolt_First + 16` |
| 17 | 2295 МГц | `cpuVolt_First + 17` |
| 18 | 2397 МГц | `cpuVolt_First + 18` |
| 19 | 2499 МГц | `cpuVolt_First + 19` |
| 20 | 2601 МГц | `cpuVolt_First + 20` |
| 21 | 2703 МГц | `cpuVolt_First + 21` |
| 22 | 2805 МГц | `cpuVolt_First + 22` |
| 23 | 2907 МГц | `cpuVolt_First + 23` |

Точка 3009 МГц намеренно **не показывается**: она исключена из UI и частотного списка Ryazha-clk v3.0.0.

### 16.5. GPU — полный построчный реестр

| Строка | Ключ | Реальное действие | Ограничение |
|---|---|---|---|
| Режим ГПУ | `KipConfigValue_marikoGpuUV` | Выбирает GPU DVFS-таблицу и доступный верх частоты. | 1459 / 1536 / 1766 МГц либо «Ручная таблица». |
| Кривая вольтажа | `KipConfigValue_GPUv` | Выбирает авто-кривую GPU либо ручную нижнюю точку. | Игнорируется в «Ручной таблице». |
| Вольтаж вершины | `KipConfigValue_hGPUv` | Напряжение верхней GPU-точки. | Авто или 750–1200 мВ. |
| Потолок рейла | `KipConfigValue_marikoGpuVmax` | Верхняя граница GPU-рейла и ручной GPU-таблицы. Меняется live в обе стороны: снижение сразу ограничивает точки, повышение сразу возвращает выбранные ранее значения. | Авто или 800–1250 мВ; sysmodule физически ограничивает точки 1200 мВ. |
| Пол вольтажа | `KipConfigValue_marikoGpuVmin` | Нижняя граница GPU-рейла, а также нижняя граница ручной GPU-таблицы. | Авто или 300–900 мВ. |
| UV нижней точки | `pMEH[19]` | Вычитает 5 мВ за шаг из нижней части авто-кривой GPU. | 0–9, то есть 0…−45 мВ; не работает в ручной таблице. |
| UV вершины | `pMEH[21]` | Вычитает 5 мВ за шаг из верхней части авто-кривой GPU. | 0–9, то есть 0…−45 мВ; не работает в ручной таблице. |
| Таблица вольтажа ГПУ | `gpuVolt_First + i` | Открывает отдельные строки 31 GPU-частоты. | Имеет смысл при режиме «Ручная таблица». |
| Сбросить к кривой RCU | все `gpuVolt_First + 0…30` | Восстанавливает 31 дефолтное значение RCU и сразу посылает их sysmodule. | Только на странице GPU-таблицы. |

### 16.6. GPU-таблица — каждая отдельная частотная строка

Каждый ползунок задаёт напряжение **именно своей** точки HiOPT. Меню дополнительно не даёт выбрать значение ниже `Пол вольтажа`, выше `Потолок рейла` или выше физического 1200 мВ.

При изменении `Пол вольтажа` или `Потолок рейла` sysmodule пересчитывает рабочую GPU-таблицу из отдельной сохранённой целевой копии, затем сразу переливает её в PCV и повторно применяет текущую GPU-частоту. Поэтому можно тюнить Vmax и Vmin **и вниз, и вверх без перезагрузки**: ранее выбранное напряжение не теряется при временном снижении потолка и возвращается, когда пользователь повышает его обратно.

| Индекс | Строка UI | Поле |
|---:|---|---|
| 0 | 307 МГц | `gpuVolt_First + 0` |
| 1 | 384 МГц | `gpuVolt_First + 1` |
| 2 | 460 МГц | `gpuVolt_First + 2` |
| 3 | 537 МГц | `gpuVolt_First + 3` |
| 4 | 614 МГц | `gpuVolt_First + 4` |
| 5 | 652 МГц | `gpuVolt_First + 5` |
| 6 | 691 МГц | `gpuVolt_First + 6` |
| 7 | 729 МГц | `gpuVolt_First + 7` |
| 8 | 768 МГц | `gpuVolt_First + 8` |
| 9 | 806 МГц | `gpuVolt_First + 9` |
| 10 | 844 МГц | `gpuVolt_First + 10` |
| 11 | 883 МГц | `gpuVolt_First + 11` |
| 12 | 921 МГц | `gpuVolt_First + 12` |
| 13 | 960 МГц | `gpuVolt_First + 13` |
| 14 | 998 МГц | `gpuVolt_First + 14` |
| 15 | 1036 МГц | `gpuVolt_First + 15` |
| 16 | 1075 МГц | `gpuVolt_First + 16` |
| 17 | 1113 МГц | `gpuVolt_First + 17` |
| 18 | 1152 МГц | `gpuVolt_First + 18` |
| 19 | 1190 МГц | `gpuVolt_First + 19` |
| 20 | 1228 МГц | `gpuVolt_First + 20` |
| 21 | 1267 МГц | `gpuVolt_First + 21` |
| 22 | 1305 МГц | `gpuVolt_First + 22` |
| 23 | 1344 МГц | `gpuVolt_First + 23` |
| 24 | 1382 МГц | `gpuVolt_First + 24` |
| 25 | 1420 МГц | `gpuVolt_First + 25` |
| 26 | 1612 МГц | `gpuVolt_First + 26` |
| 27 | 1651 МГц | `gpuVolt_First + 27` |
| 28 | 1689 МГц | `gpuVolt_First + 28` |
| 29 | 1728 МГц | `gpuVolt_First + 29` |
| 30 | 1766 МГц | `gpuVolt_First + 30` |

### 16.7. RAM, DVFS и тайминги — полный построчный реестр

| Строка | Ключ | Реальное действие | Условие |
|---|---|---|---|
| Макс. частота РАМ | `KipConfigValue_marikoEmcMaxClock` | Потолок EMC после перезагрузки; ручные напряжения VDD2/VDDQ не затирает. | Авто либо 1333–3072 МГц по готовому списку. |
| Живой вольтаж | `HocClkConfigValue_DVFSMode` | Переключает `Disabled` / `DVFSMode_Hijack` и пересобирает страницу. | При Вкл используются таблица RAM и DVFSOffset; глобальные VDD2/VDDQ скрыты. |
| Сдвиг Vmin ГПУ при RAM | `HocClkConfigValue_DVFSOffset` | Сдвигает расчётный Vmin GPU во время RAM DVFS-перехода. | Только при живом вольтаже; −80…+80 мВ, шаг 5. |
| VDD2 памяти | `KipConfigValue_commonEmcMemVolt` | Глобальное питание памяти. | Только при живом вольтаже = Выкл. |
| VDDQ шины | `KipConfigValue_marikoEmcVddqVolt` | Глобальное питание DRAM-интерфейса. | Только при живом вольтаже = Выкл. |
| Вольтаж SoC | `KipConfigValue_marikoEmcDvbShift` | Авто-сдвиг или ручное значение SoC; DVB уже имеет собственную по-частотную логику. | Всегда. |
| Таблица вольтажа РАМ | `ramVdd2_First + i`, `ramVddq_First + i` | Открывает пару VDD2/VDDQ для каждой доступной ступени EMC. | Только при живом вольтаже = Вкл. |
| Тайминги MTC | `KipConfigValue_mtcConf` | Выбирает Auto / Мои поправки / Не трогать / Своя таблица. | KIP, после перезагрузки. |
| Запас записи | `KipConfigValue_eBAL` | Добавляет к `nWR`, делая запись свободнее. | 0–5. |
| Ужатие записи | `KipConfigValue_eBAS` | Вычитает из `nWR`, делая запись туже. | 0–5. |
| Тайминги памяти | — | Открывает расширенные pMEH/sMEH/SHAITAN. | KIP, экспертная зона. |

### 16.8. RAM-таблица — все динамические слоты

Количество частотных пар берётся из **реального списка EMC именно этой консоли** и ограничено 16 слотами формата KIP. Для каждой фактической частоты появляются **две отдельные строки**: `<частота> МГц · VDD2` и `<частота> МГц · VDDQ`.

| Номер ступени | VDD2-поле | VDDQ-поле | Реальный смысл |
|---:|---|---|---|
| 0 | `ramVdd2_First + 0` | `ramVddq_First + 0` | Пара напряжений первой доступной EMC-ступени. |
| 1 | `ramVdd2_First + 1` | `ramVddq_First + 1` | Пара напряжений второй доступной EMC-ступени. |
| 2 | `ramVdd2_First + 2` | `ramVddq_First + 2` | Пара напряжений третьей доступной EMC-ступени. |
| 3 | `ramVdd2_First + 3` | `ramVddq_First + 3` | Пара напряжений четвёртой доступной EMC-ступени. |
| 4 | `ramVdd2_First + 4` | `ramVddq_First + 4` | Пара напряжений пятой доступной EMC-ступени. |
| 5 | `ramVdd2_First + 5` | `ramVddq_First + 5` | Пара напряжений шестой доступной EMC-ступени. |
| 6 | `ramVdd2_First + 6` | `ramVddq_First + 6` | Пара напряжений седьмой доступной EMC-ступени. |
| 7 | `ramVdd2_First + 7` | `ramVddq_First + 7` | Пара напряжений восьмой доступной EMC-ступени. |
| 8 | `ramVdd2_First + 8` | `ramVddq_First + 8` | Пара напряжений девятой доступной EMC-ступени. |
| 9 | `ramVdd2_First + 9` | `ramVddq_First + 9` | Пара напряжений десятой доступной EMC-ступени. |
| 10 | `ramVdd2_First + 10` | `ramVddq_First + 10` | Пара напряжений одиннадцатой доступной EMC-ступени. |
| 11 | `ramVdd2_First + 11` | `ramVddq_First + 11` | Пара напряжений двенадцатой доступной EMC-ступени. |
| 12 | `ramVdd2_First + 12` | `ramVddq_First + 12` | Пара напряжений тринадцатой доступной EMC-ступени. |
| 13 | `ramVdd2_First + 13` | `ramVddq_First + 13` | Пара напряжений четырнадцатой доступной EMC-ступени. |
| 14 | `ramVdd2_First + 14` | `ramVddq_First + 14` | Пара напряжений пятнадцатой доступной EMC-ступени. |
| 15 | `ramVdd2_First + 15` | `ramVddq_First + 15` | Пара напряжений шестнадцатой доступной EMC-ступени. |

VDD2 в каждой строке: `Авто` либо 1125–1350 мВ с физическим шагом 12,5 мВ. VDDQ в каждой строке: `Авто` либо 500–700 мВ с шагом 5 мВ. Если конкретной ступени у консоли нет, её строка не создаётся.

### 16.9. Расширенные тайминги — каждый отдельный ползунок

| Путь | Строка UI | Поле | Что конкретно меняет |
|---|---|---|---|
| Тайминги памяти | Тугость таймингов (0–8) | `pMEH[13]` | Главный уровень ужесточения; гейтит группы автоматической правки MTC. |
| Тайминги памяти | tRCD (−циклы) | `pMEH[8]` | Вычитает циклы из tRCD. |
| Тайминги памяти | tRP (−циклы) | `pMEH[9]` | Вычитает циклы из tRP. |
| Тайминги памяти | tRAS (−циклы) | `pMEH[10]` | Вычитает циклы из tRAS. |
| Тайминги памяти | SRPD | `pMEH[11]` | Правит параметр SRPD. |
| Тайминги памяти | WL набор (0=A, 1=B) | `pMEH[17]` | Выбирает один из двух наборов write-leveling. |

### 16.10. pMEH — каждый реально читаемый слот тонкой настройки

| Слот | Строка UI | Диапазон | Что читает код |
|---:|---|---:|---|
| 0 | Смещение nWR | 0–1 | Дополнительный вклад в формулу времени восстановления записи. |
| 1 | divMB супрессор | 0–4 | Регулятор divMB в логике таблицы памяти. |
| 2 | Выбор частоты MTC | 0–3 | Выбор варианта/частоты для MTC-логики. |
| 3 | Смещение записи | 0–4 | Дополнительная правка группы записи. |
| 4 | Смещение чтения | 0–4 | Дополнительная правка группы чтения. |
| 5 | Шаг tRTW | 0–4 | Дополнительный шаг read-to-write. |
| 6 | Шаг tWTR | 0–4 | Дополнительный шаг write-to-read. |
| 7 | Шаг tWTP | 0–4 | Дополнительный шаг write-to-precharge. |
| 12 | Вариант DVB-таблицы | 0–1 | Выбор варианта DVB-таблицы SoC. |
| 14 | PLL divN сдвиг | 0–24 | Сдвиг PLL делителя N. |
| 15 | ALT / заморозка | 0–1 | Переключатель ALT/заморозки в логике памяти. |
| 16 | Глобальный релакс (0/1) | 0–1 | Глобальное ослабление рассчитанных таймингов. |
| 18 | Запас EMC-брекета | 0–3 | Запас EMC-брекета; участвует в расчёте SoC/CPU-voltage. |

Слоты pMEH 8/9/10/11/13/17 вынесены на основную страницу таймингов; 19 и 21 находятся в GPU как два UV-пункта. Слоты 20/22/23 не показаны, потому что актуальный лоадер их не читает.

### 16.11. sMEH — каждый реально читаемый слот вторичных таймингов

| Слот | Строка UI | Диапазон | Что меняет |
|---:|---|---:|---|
| 0 | ARB делитель | 0–9 | Делитель арбитра памяти. |
| 6 | eZQ | 0–7 | Параметр eZQ. |
| 7 | tr DVFS | 0–1 | Флаг/режим переходов DVFS. |
| 8 | Режим (2 / >2) | 0–2 | Режим вторичной логики. |
| 9 | Флаг SSC | 0–1 | Флаг spread-spectrum clocking. |
| 10 | Латентность (сдвиг) | 0–8 | Сдвиг задержки. |
| 11 | REFRESH делитель | 0–4 | Делитель refresh. |
| 12 | Источник latencyClock | 0–1 | Выбор источника latencyClock. |
| 13 | MRS / приоритет | 0–4 | Приоритет/логика MRS. |
| 16 | Мин. частота EMC (1331/1600) | 0–1 | Минимальная базовая EMC-частота. |
| 17 | DBI | 0–3 | Режим DBI. |

Слоты sMEH 14/15/18–23 не выводятся: актуальный лоадер их не читает, поэтому UI намеренно не создаёт неработающие кнопки.

### 16.12. SHAITAN — каждый параметр ручного PCV-сплайсера

| Индекс | Строка UI | Что задаёт |
|---:|---|---|
| 0 | Активных сплайсов (0–3) | Число реально применяемых правил патчинга. `0` отключает весь SHAITAN. |
| 1 | Спл1 искомое | Значение слова, которое ищет правило №1. |
| 2 | Спл1 смещение | Смещение слов от найденного значения до проверяемого места. |
| 3 | Спл1 ожидаемое | Слово, которое должно быть на указанном смещении. |
| 4 | Спл1 замена | Новое слово, записываемое после успешной проверки правила №1. |
| 5 | Спл2 искомое | Искомое слово правила №2. |
| 6 | Спл2 смещение | Смещение правила №2. |
| 7 | Спл2 ожидаемое | Ожидаемое слово правила №2. |
| 8 | Спл2 замена | Слово-замена правила №2. |
| 9 | Спл3 искомое | Искомое слово правила №3. |
| 10 | Спл3 смещение | Смещение правила №3. |
| 11 | Спл3 ожидаемое | Ожидаемое слово правила №3. |
| 12 | Спл3 замена | Слово-замена правила №3. |
| 13 | Множитель кол-ва (x30) | Множитель количества/поиска для ручного патчинга. |

Все 14 полей имеют диапазон 0–63. При `Активных сплайсов = 0` лоадер не выполняет ручной патчинг.

### 16.13. Чиптюнинг — каждый отдельный пункт

| Модель/рейл | Строка UI | Ключ | Реальное действие |
|---|---|---|---|
| Все | Включить | `ChiptuneEnable` | Главный переключатель применения чиптюнинга. |
| Mariko CPU M4 | Прямой ШИМ | `ChiptuneCpuFpwm` | Forced PWM CPU-рейла. |
| Mariko CPU M4 | Лимит тока | `ChiptuneCpuPeakIlim` | Пиковый current limit CPU-рейла. |
| Mariko CPU M4 | Частота ШИМ | `ChiptuneCpuSwFreq` | Switching frequency CPU-рейла. |
| Mariko все рейлы | Включить | `ChiptuneLoopComp` | Компенсация управляющей петли. |
| Mariko DRAM M3 | Прямой ШИМ | `ChiptuneDramFpwm` | Forced PWM DRAM-рейла. |
| Mariko DRAM M3 | Лимит тока | `ChiptuneDramPeakIlim` | Пиковый current limit DRAM-рейла. |
| Mariko DRAM M3 | Частота ШИМ | `ChiptuneDramSwFreq` | Switching frequency DRAM-рейла. |
| Mariko GPU M1 | Прямой ШИМ | `ChiptuneGpuFpwm` | Forced PWM GPU-рейла. |
| Mariko GPU M1 | Лимит тока | `ChiptuneGpuPeakIlim` | Пиковый current limit GPU-рейла. |
| Mariko GPU M1 | Частота ШИМ | `ChiptuneGpuSwFreq` | Switching frequency GPU-рейла. |
| Mariko все рейлы | Скорость | `ChiptuneSlewProfile` | Скорость нарастания напряжения: Авто/5/10/20/40 мВ/мкс. |
| Erista CPU | ЦПУ — Прямой ШИМ | `ChiptuneEristaFpwmCpu` | Forced PWM CPU на Erista. |
| Erista GPU | ГПУ — Прямой ШИМ | `ChiptuneEristaFpwmGpu` | Forced PWM GPU на Erista. |
| Картридж GCA | Напряжение | `ChiptuneCartridgeVolt` | Питание картриджа: Авто либо 2800–3300 мВ. |

### 16.14. Общие, экспериментальные, экран, VRR, Auto OC и безопасность — каждая строка

| Путь | Строка UI | Ключ | Реальное действие / условие |
|---|---|---|---|
| Общие | Вольтаж РАМ: показ | `RAMVoltDisplayMode` | Выбор отображаемой величины VDD2/VDDQ. Только Mariko; на питание не влияет. |
| Общие | Единицы частоты РАМ | `RamDisplayUnit` | МГц / MT/s / оба формата. Только отображение. |
| Общие | Интервал опроса | `PollingIntervalMs` | Частота опроса sysmodule, 50–1000 мс. |
| Экспериментальные | Mariko Middle Frequencies | `MarikoMiddleFreqs` | Включение промежуточных Mariko-ступеней. Только Mariko. |
| Экспериментальные | Live CPU UV | `LiveCpuUv` | Экспериментальный живой CPU undervolt. Только Mariko. |
| Экспериментальные | Планировщик ГПУ | `GPUSchedulingMethod` | Метод override GPU scheduler: INI или NV Service. |
| Экспериментальные | Измерение частоты РАМ | `MemoryFrequencyMeasurementMode` | Источник измерения: PLL или Actmon. |
| Экспериментальные | Ток зарядки | `BatteryChargeCurrent` | Устанавливает зарядный ток; список зависит от модели. |
| Экспериментальные | Предел тока зарядки | `InputCurrentLimit` | Лимит входного тока; UI выделяет риск выше 1500 мА. |
| Экспериментальные | Цветовой профиль | `AulaDisplayColorPreset` | Выбор панели Aula/OLED. Только Aula. |
| Экран | Менять частоту обновления | `OverwriteRefreshRate` | Разрешает профилям/Overrides управлять герцовкой; открывает Display/VRR строки. |
| Экран | Настройки VRR-Авто | — | Открывает четыре параметра VRR-Авто ниже; не включает режим сам. |
| Экран | Макс. Гц (портатив) | `MaxDisplayClockH` | Верхняя ручная граница герцовки в портативе. Только не-Retro; модель-зависимый диапазон. |
| Экран | Вольтаж дисплея | `DisplayVoltage` | 800–1200 мВ панели. Только не-Aula; опасный параметр. |
| VRR-Авто | Мин. Гц | `VrrAutoMinHz` | Нижняя граница адаптивной герцовки. |
| VRR-Авто | Макс. Гц | `VrrAutoMaxHz` | Верхняя граница адаптивной герцовки. |
| VRR-Авто | Агрессивность | `VrrAutoAggressive` | Скорость/готовность предиктора менять Hz. |
| VRR-Авто | Порог скачка | `VrrAutoSpikeFps` | FPS-разница, классифицируемая как резкое изменение. |
| VRR-Авто | Запас над FPS (Гц) | `VrrAutoMargin` | Hz, удерживаемые над измеренным FPS. |
| VRR-Авто | Задержка понижения (тики) | `VrrAutoDownHold` | Сколько тиков ждать до снижения Hz. |
| Ряженка-Авто | Включить авторазгон | `AutoOcEnable` | Запускает автоматический подбор CPU/GPU под FPS. |
| Ряженка-Авто | Целевой FPS | `AutoOcTargetFps` | FPS-цель алгоритма. |
| Ряженка-Авто | Допуск (кадров) | `AutoOcTolerance` | Разрешённое отклонение от цели. |
| Ряженка-Авто | Алгоритм | `AutoOcAlgo` | Выбор стратегии `Цикл` / `Смарт`. |
| Ряженка-Авто | Лимит темп. ЦП | `AutoOcTempCapCpu` | Потолок температуры CPU, приоритетнее FPS-цели. |
| Ряженка-Авто | Лимит темп. ГП | `AutoOcTempCapGpu` | Потолок температуры GPU, приоритетнее FPS-цели. |
| Ряженка-Авто | Лимит TDP | `AutoOcTdpCapMw` | Предел мощности, приоритетнее FPS-цели. |
| Ряженка-Авто | Мин. ЦП | `AutoOcCpuMinMhz` | Нижняя граница CPU для алгоритма. |
| Ряженка-Авто | Макс. ЦП | `AutoOcCpuMaxMhz` | Верхняя граница CPU для алгоритма. |
| Ряженка-Авто | Мин. ГП | `AutoOcGpuMinMhz` | Нижняя граница GPU для алгоритма. |
| Ряженка-Авто | Макс. ГП | `AutoOcGpuMaxMhz` | Верхняя граница GPU для алгоритма. |
| Ряженка-Авто | Держать РАМ на максимуме | `AutoOcRamPin` | Не даёт автоматике опустить RAM со своего верхнего уровня. |
| Говернёр | Мин. частота ЦПУ | `CpuGovernorMinimumFreq` | Минимум CPU-governor: 510/612/714/816/918/1020 МГц. |
| Безопасность | Снять потолки частот | `UncappedClocks` | Снимает программные частотные ограничения. |
| Безопасность | Сброс при перегреве | `ThermalThrottle` | Возвращает частоты к стоку при перегреве. |
| Безопасность | Порог троттлинга | `ThermalThrottleThreshold` | Порог 50–85 °C для предыдущего механизма. |

### 16.15. Полнота и источник правды

Реестр покрывает все создаваемые `ListItem`, `addConfigButton`, `addConfigToggle` и `addConfigTrackbar` в `misc_gui.cpp`, а также строки главного экрана, профилей и временных значений. Динамические GPU/CPU/RAM-таблицы перечислены по каждому слоту. Параметры, которых код не читает и поэтому не выводит в UI, не являются «пропущенными кнопками»: они намеренно исключены из интерфейса.

**Исходники для проверки реестра:** `main_gui.cpp:43–139`, `app_profile_gui.cpp:47–501`, `global_override_gui.cpp:328–439`, `misc_gui.cpp:655–2343`, `config_info_strings.cpp:181–549`.


---

## Связь по исходникам проекта

Если нужен исходный код проекта, требуется уточнить реализацию конкретного параметра или есть вопрос по развитию Ryazha-clk, обратитесь к автору проекта в Telegram:

> **[Telegram: @Dimas880312](https://t.me/Dimas880312)**
